export const pageBoxContainerStyle = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  minHeight: 'calc(100vh - 64px)',
  flexDirection: 'column',
  gap: '1rem',
}

export const notFoundPageBoxStyle = {
  ...pageBoxContainerStyle,
  gap: 2,
}

export const iconSx = { fontSize: 18, color: '#666' }
export const bigIconSx = { fontSize: 24, color: '#666' }
